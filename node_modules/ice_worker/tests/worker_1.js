'use strict';
var ice_worker = require('ice_worker');
var q = require('ice_q');

ice_worker.register_worker('hello', {prefetchCount:1}, function(a,b,c){
  console.log('WORKER 1', a, b, c);
  return [a,b,c];
});
