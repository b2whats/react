'use strict';
var ice_worker = require('ice_worker');

/**
 * например в это время идет апдейт данных и воркер передает управление другому воркеру 
 */

ice_worker.register_worker('hello', {prefetchCount:6}, function(a,b,c){
  return ice_worker.kPASS_TO_OTHER_WORKER;
});